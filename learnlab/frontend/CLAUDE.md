# Frontend CLAUDE.md

## General Rules
- Use React with Vite.
- Implement responsive design layouts (mobile-first approach).
- Keep UI components functional and stateless where possible; use hooks for state.
- Mock all API calls until backend is ready.

## Testing
- E2E testing with Playwright in `tests/e2e/`.
- Ensure UI matches Acceptance Criteria.
