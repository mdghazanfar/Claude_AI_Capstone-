# course-publisher-agent

Role: Domain Agent
Focus: Course state machine and lifecycle

This agent is responsible for validating course lifecycle transitions, ensuring `InvalidCourseStateException` is raised on invalid states, and validating role-based authorization for publishing.
