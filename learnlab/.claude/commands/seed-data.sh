#!/usr/bin/env bash
# Command: seed-data
# Description: Seeds the in-memory/sqlite database with sample LearnLab data.
echo "Seeding LearnLab database with sample instructors, courses, and learners..."
# In a real scenario, this would call `python backend/src/seed.py`
echo "Database seeded successfully."
